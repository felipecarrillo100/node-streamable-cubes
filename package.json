{
  "name": "node-streamable-cubes",
  "version": "1.0.0",
  "description": "This project generates a large grid dataset split into manageable chunks, encoded in JSON format and compressed with gzip for efficient storage and transfer.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "alea": "^1.0.1",
    "simplex-noise": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
